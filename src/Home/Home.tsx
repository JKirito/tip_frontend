import React, { useEffect } from 'react';
// import { checkValidation } from '../utils/ProtectedRoute';

const Home = () => {
  useEffect(() => {
    //   checkValidation();
  }, []);
  return <div>Home</div>;
};

export default Home;
